# Frontend (Vite) dev server settings
PORT=3000
HOST=0.0.0.0

# Backend API base URL used by the frontend
# The app reads this as import.meta.env.VITE_API_BASE_URL
# Default backend: http://localhost:3001
VITE_API_BASE_URL=http://localhost:3001
